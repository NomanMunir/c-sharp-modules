/*
 * Module 05 - Exercise 01: File Manager
 * Implement: Contact, ContactManager (IDisposable), JSON persistence
 */

using System.Text.Json;

// TODO: Create Contact class
// TODO: Create ContactManager class implementing IDisposable

class Program
{
    static void Main(string[] args)
    {
        /* Uncomment when ready
        using (ContactManager cm = new ContactManager("contacts.json"))
        {
            cm.AddContact("Alice", "alice@example.com");
            cm.AddContact("Bob", "bob@42.fr");
            cm.ListContacts();
        }

        // Verify persistence:
        using (ContactManager cm2 = new ContactManager("contacts.json"))
        {
            Console.WriteLine("\n=== Loaded from file ===");
            cm2.ListContacts();
        }
        */
    }
}
