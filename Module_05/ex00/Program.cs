/*
 * Module 05 - Exercise 00: Custom Exceptions & Error Handling
 * Implement: GradeTooHighException, GradeTooLowException, Bureaucrat
 */

// TODO: Create GradeTooHighException : Exception
// TODO: Create GradeTooLowException : Exception
// TODO: Create Bureaucrat class

class Program
{
    static void Main(string[] args)
    {
        /* Uncomment when ready
        try
        {
            Bureaucrat b1 = new Bureaucrat("Alice", 1);
            Console.WriteLine(b1);
            b1.IncrementGrade();  // Should throw
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error: {ex.Message}");
        }

        try
        {
            Bureaucrat b2 = new Bureaucrat("Bob", 151);  // Should throw
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error: {ex.Message}");
        }

        Bureaucrat b3 = new Bureaucrat("Charlie", 75);
        Console.WriteLine(b3);
        b3.IncrementGrade();
        Console.WriteLine(b3);
        */
    }
}
